// Drizzle schema — intentionally EMPTY in the base skeleton.
//
// There are deliberately zero domain tables (no users, rides, bookings, etc.).
// Drizzle + drizzle-kit are fully wired so the first real migration is a
// `pnpm db:generate` away.
//
// TODO: domain tables go here, e.g.
//   export const users = pgTable('users', { ... });

export {};
