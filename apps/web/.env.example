# --- apps/web environment ---
# Copy to .env.local. Validated by Zod (src/lib/env.ts).
# NEXT_PUBLIC_* values are inlined into the browser bundle at build time.

NEXT_PUBLIC_API_URL=http://localhost:3001
