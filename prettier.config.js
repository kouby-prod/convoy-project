// Root Prettier config — re-exports the shared monorepo config so a single
// `pnpm format` at the root formats every app and package consistently.
export { default } from '@carpool/config/prettier';
